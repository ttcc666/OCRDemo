<Window x:Class="OCRDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="多引擎 OCR 识别工具" Height="750" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部按钮区域 -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 第一行：引擎选择 -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="🔧 OCR 引擎：" VerticalAlignment="Center" FontSize="14" FontWeight="Bold"/>
                <ComboBox Name="CmbOcrEngine" Width="350" Height="35" FontSize="13"
                          SelectionChanged="CmbOcrEngine_SelectionChanged" Margin="10,0,0,0"/>
                <Button Name="BtnInitEngine" Content="🔄 初始化引擎"
                        Width="120" Height="35" FontSize="13"
                        Click="BtnInitEngine_Click" Margin="10,0,0,0" IsEnabled="False"/>
            </StackPanel>

            <!-- 第二行：操作按钮 -->
            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <Button Name="BtnSelectImage" Content="📁 选择图片"
                        Width="120" Height="40" FontSize="14"
                        Click="BtnSelectImage_Click" Margin="0,0,10,0"/>
                <Button Name="BtnRecognize" Content="🔍 识别文字"
                        Width="120" Height="40" FontSize="14"
                        Click="BtnRecognize_Click" IsEnabled="False" Margin="0,0,10,0"/>
                <Button Name="BtnClear" Content="🗑️ 清空"
                        Width="120" Height="40" FontSize="14"
                        Click="BtnClear_Click" Margin="0,0,10,0"/>
                <TextBlock Name="TxtStatus" Text="请选择并初始化 OCR 引擎"
                           VerticalAlignment="Center" Margin="20,0,0,0"
                           FontSize="14" Foreground="#666"/>
            </StackPanel>
        </Grid>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：图片预览 -->
            <Border Grid.Column="0" BorderBrush="#CCCCCC" BorderThickness="1"
                    CornerRadius="5" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="📷 图片预览" FontSize="16"
                               FontWeight="Bold" Margin="0,0,0,10"/>

                    <Border Grid.Row="1" Background="#F5F5F5" CornerRadius="3">
                        <Grid>
                            <!-- 占位文本 -->
                            <TextBlock Name="TxtPlaceholder"
                                       Text="请选择图片文件&#x0a;支持格式：JPG, PNG, BMP"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       TextAlignment="Center"
                                       FontSize="14"
                                       Foreground="#999"/>

                            <!-- 图片显示 -->
                            <Image Name="ImgPreview"
                                   Stretch="Uniform"
                                   Visibility="Collapsed"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- 分隔符 -->
            <GridSplitter Grid.Column="1" Width="5"
                          HorizontalAlignment="Stretch"
                          Background="Transparent"/>

            <!-- 右侧：识别结果 -->
            <Border Grid.Column="2" BorderBrush="#CCCCCC" BorderThickness="1"
                    CornerRadius="5" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="📝 识别结果" FontSize="16"
                               FontWeight="Bold" Margin="0,0,0,10"/>

                    <Border Grid.Row="1" Background="#F5F5F5" CornerRadius="3">
                        <TextBox Name="TxtResult"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 FontSize="14"
                                 Padding="10"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 IsReadOnly="True"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <!-- 底部状态栏 -->
        <Border Grid.Row="2" BorderBrush="#CCCCCC" BorderThickness="0,1,0,0"
                Padding="0,10,0,0" Margin="0,10,0,0">
            <StackPanel>
                <TextBlock Name="TxtInfo"
                           Text="多引擎 OCR 识别工具 | 支持 PaddleOCR 和 Tesseract"
                           FontSize="12" Foreground="#666" Margin="0,0,0,5"/>
                <TextBlock Name="TxtEngineInfo" Text=""
                           FontSize="11" Foreground="#999"/>
            </StackPanel>
        </Border>

        <!-- 加载遮罩层 -->
        <Grid Name="LoadingOverlay" Grid.RowSpan="3"
              Background="#80000000" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20"/>
                <TextBlock Name="TxtLoading" Text="正在初始化模型..."
                           Foreground="White" FontSize="14"
                           HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
